description: >
  Executes test workflow generated by create-config job.

executor: continuation/default

steps:
  - attach_workspace:
      at: /tmp/workspace
  - continuation/continue:
      configuration_path: /tmp/workspace/generated.yml # use newly generated config to continue
